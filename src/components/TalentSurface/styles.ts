import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  pendingContainer: {
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  pendingText: {
    fontStyle: 'italic',
    opacity: 0.7,
  },
});
