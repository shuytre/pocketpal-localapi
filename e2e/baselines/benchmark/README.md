# Benchmark baselines

Captured at `bench: {pp:256, tg:64, pl:1, nr:3, inter_cell_settle_ms:30000}` with the per-backend rules and `mallopt(M_PURGE_ALL)` purge APK applied. See `COVERAGE.md` for per-device cell counts, recovery story, and operational quirks.
