# 提示词 —— 本地 LLM 推理 App 的性能与调度调优

> 直接整份复制使用。这是一份**实现要求**，不是报告。

---

WorkBuddy 改造 PocketPal AI 详细任务提示词
 
适用模式：Plan 模式（复杂多文件改造，先规划再执行）
工作目录：PocketPal AI 源码根目录（已完成  git clone https://github.com/a-ghorbani/pocketpal-ai  并安装完基础依赖）
 
 
 
一、任务总览
 
目标
 
对开源项目 PocketPal AI 进行二次开发，直接在这个源码文件夹里面修改，在不破坏原有离线对话、模型管理、基准测试等核心功能的前提下，新增局域网 OpenAI 兼容 HTTP API 服务。实现手机本地加载 GGUF 模型后，同一局域网内的电脑/设备可以通过标准 OpenAI 接口调用该模型，用于对接 Cloud Code 类编码 Agent，服务逆向工程课程课题。
 
背景
 
1. PocketPal AI 是基于 React Native + llama.rn（llama.cpp 的 RN 封装） 开发的移动端离线 LLM 应用，原生支持加载 GGUF 格式模型，无 Termux 容器层性能损耗。

2. 原有功能仅支持 APP 内本地对话，无对外 HTTP 接口，无法被局域网其他设备调用。

3. 本次改造仅用于学术课程研究，最终部署在红米 K20（安卓 10、骁龙 730、8GB 内存）。
 
最终交付
 
可编译的安卓 APK 源码，启动 APP 并加载模型后，可通过  http://手机局域网IP:8080/v1/chat/completions  调用，兼容标准 OpenAI SDK 与编码 Agent。
 
 
 
二、核心改造需求（按优先级）
 
1. 内嵌轻量 HTTP 服务引擎
 
- 在 React Native 原生层引入 nodejs-mobile，在 APP 后台常驻一个 Node.js 进程，基于 Express 框架启动 HTTP 服务。

- 监听地址： 0.0.0.0:8080 （支持局域网访问，而非仅 127.0.0.1）。

- 服务随 APP 启动自动初始化，模型加载完成后自动进入可用状态；模型卸载时服务保留但返回忙碌状态。

- 端口可在 APP 设置页自定义，默认 8080。
 
2. 实现标准 OpenAI 兼容接口
 
仅需实现核心聊天补全接口，完全兼容 OpenAI  /v1/chat/completions  协议规范：
 
- 请求方式：POST

- 请求体：支持  model 、 messages 、 temperature 、 stream 、 max_tokens  核心字段，其余字段兼容忽略不报错。

- 响应格式：

- 非流式：返回标准 OpenAI 响应 JSON 结构，包含  id 、 object 、 created 、 choices 、 usage 。

- 流式（ stream=true ）：必须支持 SSE 流式输出，按  data: {...}  格式逐块返回 token，兼容编码 Agent 流式打字体验，最终返回  data: [DONE] 。

- 模型名称映射： model  字段传入值自动映射到当前 APP 已加载的 GGUF 模型，无需多模型并发。
 
3. 复用原有推理能力，不重复造轮子
 
- 禁止重新实现 llama.cpp 推理逻辑，直接复用 PocketPal 现有  llama.rn  封装的推理接口。

- 将 HTTP 请求的参数（prompt、temperature、max_tokens 等）透传给已加载的模型实例，推理结果按流式/非流式返回。

- 上下文长度复用 APP 设置中的  ctx-size  配置，默认 2048，与本地对话保持一致。
 
4. 安卓权限与兼容性
 
- 在  AndroidManifest.xml  中补充必要权限：
 

- 兼容安卓 10（API 29），不申请高版本安卓的附近设备、后台运行等多余权限。

- APP 退到后台时，服务不被系统杀死的优化（加入前台服务通知可选，最低优先级实现）。
 
5. 界面与配置增强
 
- 在 APP「设置」页面新增「局域网 API 服务」板块：

- 服务开关：手动开启/关闭 HTTP 服务

- 端口号输入框：默认 8080

- 本机局域网 IP 显示：自动获取并展示，方便用户填写端点

- 运行状态指示：显示「监听中/等待模型加载/已停止」

- 不破坏原有模型管理、聊天、基准测试页面的布局与功能。
 
6. 附加功能（低优先级，有时间再实现）
 
- 简单的访问密钥（API Key）校验，请求头  Authorization: Bearer xxx ，默认关闭，可在设置中配置。

- CORS 跨域支持，允许浏览器端直接调用。

- 访问日志简单输出到 APP 控制台。
 
 
 
三、技术约束与规范
 
1. 性能优先：HTTP 服务层尽量轻量化，减少额外 CPU 与内存占用，不能明显降低模型推理 token 速度。

2. 稳定性：多轮连续请求不崩溃、不闪退；模型加载中发起请求返回明确的 503 错误，而非直接崩溃。

3. 代码规范：

- 新增代码添加清晰注释，说明功能与调用逻辑。

- 不随意升级  llama.rn  依赖版本，避免破坏老设备兼容性。

- 尽量复用现有项目的状态管理与工具函数。

4. 安全声明：在设置页与启动说明中标注「本功能仅用于学术研究，禁止用于非法用途」。
 
 
 
四、实施步骤（请按顺序执行）
 
第一步：项目结构分析与依赖评估
 
1. 扫描当前 PocketPal 源码目录，梳理核心模块：模型加载层、llama.rn 调用接口、页面路由、状态管理。

2. 评估  nodejs-mobile-react-native  依赖的引入方式与兼容性，确认与现有 React Native 版本匹配。

3. 输出改造方案与文件修改清单，确认无误后再开始编码。
 
第二步：核心 HTTP 服务实现
 
1. 引入 nodejs-mobile 依赖，编写 Express 服务入口脚本。

2. 实现  /v1/chat/completions  接口，对接 llama.rn 推理函数，先完成非流式响应。

3. 开发 SSE 流式输出逻辑，逐 token 返回，兼容 OpenAI 流式协议。

4. 增加错误处理：模型未加载、参数错误、推理异常的对应 HTTP 状态码与报错信息。
 
第三步：安卓权限与原生配置
 
1. 修改安卓清单文件，补充网络权限。

2. 配置 nodejs-mobile 安卓端构建脚本，确保打包时包含服务脚本。

3. 适配安卓 10 后台运行限制，保证服务在 APP 前台时稳定运行。
 
第四步：设置页 UI 改造
 
1. 在设置页面新增局域网服务配置板块，实现开关、端口输入、IP 显示、状态指示。

2. 绑定 UI 操作与服务启停逻辑，配置项持久化保存。
 
第五步：联调与测试
 
1. 编译安卓 Debug 包，验证基础功能：模型加载、本地对话正常。

2. 开启服务，用同一局域网电脑通过 curl 测试非流式与流式接口。

3. 验证编码 Agent 接入：配置自定义 OpenAI 端点，测试连续多轮对话。
 
 
 
五、验收标准
 
1. ✅ 原有功能全部正常：模型下载/导入、本地聊天、基准测试无异常。

2. ✅ 局域网内可通过  http://IP:8080/v1/chat/completions  正常调用。

3. ✅ 支持  stream=true  流式输出，token 逐块返回，延迟正常。

4. ✅ 标准 OpenAI SDK / Cloud Code Agent 可直接配置接入，无需修改代码。

5. ✅ 连续 10 轮多轮对话不崩溃、内存无明显泄漏。

6. ✅ 红米 K20 安卓 10 上可正常安装运行，无权限报错。
 
 
 
六、注意事项
 
1. 不要修改 llama.rn 底层推理逻辑，所有改造集中在应用层与 HTTP 封装层。

2. 优先保证功能可用与稳定，UI 美化与附加功能后置。

3. 每完成一个模块保留可回滚的提交，避免整体改造失败。

4. 所有新增第三方依赖均使用稳定版本，不使用 alpha/beta 版。

5. 来做完之后请直把稳定版apk（或zip）发送给我



把本地模型给它整合成一个API，可以直接在同局域网下的其他电脑连接。设置里面要必须要有base url，还有API key可以自定义的，或者是还有一些其他的提升模型能力的设置。然后还有管理界面，就是显示什么时候调用，调用了多少。

## 任务

我要在一个**已经跑通本地 LLM 推理的 Android App**（React Native + `llama.rn` /
llama.cpp）上，做一轮**性能与调度调优**。目标设备是**红米 K20（Snapdragon 730，
2×Cortex-A76 + 6×Cortex-A55，8GB RAM）**，设备**未 root**。

请实现下面全部条目。每一条都写清了「要做什么」和「为什么这么做」，
请按技术要点实现，不要用等价但效果更差的简化方案替换。

---

## 全局约束（先读，适用于所有条目）

1. **必须区分「请求值」和「实际生效值」。**
   未 root 的设备上，有些系统调用必然失败。代码要**尝试、测量、回读**，
   并把**内核返回的真实值**暴露到界面上。绝不能因为调用没抛异常就显示「已生效」。
   这不是保守，是正确性 —— 用户会照着界面判断调优有没有用。

2. **调优是旁路增强，失败不能影响主功能。**
   所有调优调用都要包在异常收敛里（包括 `UnsatisfiedLinkError` 这类 `Error`，
   只 catch `Exception` 会漏掉）。原生库缺失时退化为「未调优」，而不是崩溃。

3. **不要静默降级。**
   做不到就明确说做不到，并说明原因（例如「RLIMIT_NICE=0/0，非 root 无法提权」）。
   宁可显示「未生效」，也不要显示一个笼统的「已开启」。

---

## 需求 1 · CPU 亲和性绑定（强制绑 A76 大核）

### 要做什么

把推理线程绑定到设备**最高频的核心簇**（K20 上是 2 个 A76），
不要让它摊到 6 个 A55 上。

### 为什么

`Snapdragon 730` 是 2×A76 @2.2GHz + 6×A55 @1.8GHz。llama.cpp 默认按 `n_threads`
铺满所有核，但 **ggml 的 matmul 有同步屏障：一次计算的耗时由最慢的那个线程决定**。
把 8 个线程摊到 2 大 + 6 小上，实际是让 6 个慢线程拖住 2 个快线程。

### 技术要点

- **用 `sched_setaffinity(2)`**。它对**本进程自己的线程**不需要任何特权
  （内核只检查 pid 是否属于调用者）—— 这是**未 root 下唯一真正能改变调度位置**
  的机制。对比：`setpriority` 降 nice、`sched_setscheduler` 切实时类都要
  `CAP_SYS_NICE`，在非 root 下必然失败。
- **大核靠频点聚类判定，不要硬编码 `cpu6,cpu7`**：
  ```
  读 /sys/devices/system/cpu/possible              → 内核实际放行了哪些 CPU
  读 cpu<N>/cpufreq/cpuinfo_max_freq               → 每个核的最高频点
  按频点聚簇，取最高频那一簇                        → 即"大核"
  ```
  理由：同代 SoC 的簇布局在 6 核 / 8 核 / 带热插拔的机型上并不一致，硬编码会把
  线程绑到错误的核、甚至不存在的核上（`sched_setaffinity` 返回 `EINVAL`）。
- **单一频簇时返回空数组**，语义是「无需绑定」而不是「绑定失败」。
  UI 文案也要区分这两者。
- **绑定 llama.cpp 的工作线程**：那些线程由原生库自己创建，Java 侧拿不到 tid。
  但 `/proc/self/task/<tid>/comm` 对同进程始终可读，所以：
  - 周期性扫描 `/proc/self/task`，读每个 tid 的 `comm`
  - 按**前缀白名单**匹配（`ggml` / `llama` 等）
  - **必须用白名单而不是黑名单**：绑错线程（例如把 `RenderThread` 拉到大核）
    会和推理抢同一簇，比不绑更糟。宁可漏绑。
  - 在推理进行中**周期性重绑**（建议限流 250ms）：worker 线程按需创建又回收，
    只在启动时绑一次会漏掉后续新建的
- **提供兜底路径**：`sched_setaffinity(0)` 失败时，改用显式 `gettid()` 再试一次。
  个别厂商内核对 `pid=0` 的处理有差异，走一次显式路径能把「内核真不允许」和
  「参数形态不兼容」区分开 —— 两者的排查方向完全不同。

### 验收

界面上显示的**当前亲和掩码**是真实的大核 id（K20 上应为 `6,7`，
由 `sched_getaffinity` 回读，不是我们请求的值）。

---

## 需求 2 · 推理线程优先级（未 root 下尽可能最高）

### 先明确边界（这一条必须如实告知用户）

未 root 的 Android 上：

| 手段 | 是否有效 |
|------|----------|
| 绑到大核（需求 1） | **有效** —— `sched_setaffinity` 免特权 |
| `n_threads` 对齐到物理大核数 | **有效** |
| 专用推理线程（需求 4） | **有效** |
| 给后台线程设正 nice（让路） | **有效** —— 上调 nice 永远允许 |
| `oom_score_adj` 下调 | **部分** —— 内核按进程 adj 下限截断 |
| **nice 降到 0 以下** | **无效** —— 需要 `CAP_SYS_NICE` |
| **`SCHED_FIFO` / `SCHED_RR`** | **无效** —— 同上 |
| 改 CPU governor | **无效** —— `/sys/.../scaling_governor` 需 root |
| 关系统省电策略 | **无效** —— 需用户在系统设置里手动改 |

**所以「最高优先级」的实际含义是：把能做的四件事做满，并把做不到的如实说明。**

### 技术要点

- **权威判据是 `getrlimit(RLIMIT_NICE)`**，先读它再谈能不能提权。
  它的语义容易写错，记清楚：
  ```
  允许的最低 nice = 20 - rlimit_nice_cur

    cur = 0   → 最低 nice 20   （连默认的 0 都提不上去，Android 应用常态）
    cur = 20  → 最低 nice 0
    cur = 40  → 最低 nice -20  （完整提权，需 CAP_SYS_NICE 或 root）
  ```
- **先尝试 `setrlimit` 把软限抬到硬限** —— 这是唯一不需要特权的操作。
  抬升前后的数值都要报出来，让用户看到"为什么提不上去"。
- **nice 用阶梯下探，不要只设一次 -20**：
  从最有价值的 `-20` 起，按 `-19 / -16 / -12 / -10 / -8 / -5 / -4 / -2` 逐级放宽，
  **命中即停**。不同 ROM 对 `RLIMIT_NICE` 与 sched 组的放行程度不同，
  有的允许 -5 却不允许 -20；一次到位只能拿到「全有或全无」，
  阶梯能拿到该设备**实际能给到的最好值**。
- **每次 setter 后回读**：报告里同时显示「请求值 / 下探到哪一级 / 实际生效值 /
  `RLIMIT_NICE` 软硬限 / 当前调度类」。
- **`SCHED_FIFO` 默认不要开**。一旦成功，线程会抢占整个核，在网络 + UI 并发
  场景下反而增加输入延迟。做成显式开关，默认关闭。
- **后台线程让路**：给下载 / 网络 / 协程线程设**正 nice**（永远允许），
  但必须**白名单**匹配明确的后台前缀，**绝不能碰 RN 线程** ——
  推理本身跑在 JS 线程上，给它降优先级会直接拖慢推理。

---

## 需求 3 · mlock 内存锁定（模型常驻物理内存）

### 要做什么

模型加载完成后，把它的权重页锁定在物理内存里，避免推理过程中被换出。

### 技术要点 —— 这一条最容易做错

**编译期**：llama.cpp 的 mlock 支持默认就编进去了（没有开关能关掉），
所以「编译期开启」的落点是 **`use_mlock=true` 这个上下文初始化参数**。

**但光设这个参数没用。** llama.cpp 调 `mlock` 用的是**进程级
`RLIMIT_MEMLOCK`**，而 Android 应用的默认软硬限通常只有 **64KB**。
于是 llama.cpp 的 mlock 会在几毫秒内因 `ENOMEM` 失败 ——
而它**只往 stderr 打一行警告**，App 内完全看不到。
这就是「看起来开了 mlock，实际一点没锁住」的根因。

**所以运行时那一半必须自己补**：

1. **把 `RLIMIT_MEMLOCK` 软限抬到硬限**（唯一不需要特权的操作），
   抬升前后的数值都报出来。注意硬限本身在被提权前也抬不上去，
   所以要**如实说明"锁不满"**，不要装作能锁满。
2. **自己 `mmap` 模型文件并分片 `mlock`**：
   - 必须用 **`MAP_SHARED`**。关键理由：`MAP_SHARED` 映射的是**页缓存里的
     同一批物理页**，llama.cpp 自己 mmap 的同一文件也指向这些页 ——
     因此锁定**不额外占一份内存**，只是让这些页不会被回收。
   - **映射必须一直持有**：`munmap` 会同时释放 mlock 计数，
     所以要保留映射的地址与长度。
   - **分片 mlock**（建议 1MB 一片）：一次性 mlock 一大段在 `ENOMEM` 时
     无法知道锁到了哪里，分片后至少能精确报出"锁住了多少"。
   - 先 `posix_fadvise(POSIX_FADV_WILLNEED)` 预热：冷文件直接 mlock 会让
     调用线程在 IO 上阻塞很久。
3. **锁不住的部分退化为 `madvise(MADV_WILLNEED)` 预取** —— 不保证常驻，
   但能消掉推理过程中的大部分缺页，是权限受限下能拿到的最好结果。

**不要用 `mlockall()`。** 那会把整个地址空间（含 JS 引擎堆、纹理、Dex）
一起锁住，在 8GB 机型上直接触发低内存回收，与目标相反。

### 验收

界面区分**三个数字**，而不是一个笼统的「已开启常驻」：

- `已锁定 1024MB 到物理内存`
- `已锁定 64KB，其余 1023.9MB 仅预取到页缓存`
- `mlock 受限（RLIMIT_MEMLOCK=64KB），已预取 1024MB`

**补充**：`oom_score_adj` 也按同样原则处理 —— 非 root 进程通常只能上调
（更早被杀），下调会被内核拒绝，所以要返回**回读到的值**而非请求值。

---

## 需求 4 · 独立 Native 推理线程 + 最小化跨桥拷贝

### 要做什么

1. 推理跑在一个**常驻、已调优**的专用线程上，而不是每个请求临时 `new Thread`。
2. 把「模型 → 客户端」链路上的跨线程/跨桥调用次数压下来。

### 为什么不能只用临时线程

`new Thread(...)` 有两个代价：
- **绑不上大核**：`sched_setaffinity(0)` 只作用于调用线程，而临时线程在
  `run()` 里才第一次存在。要在每个请求开头重新探测拓扑、重新绑定，
  等于把 sysfs 读取放进延迟敏感路径。
- **继承不到优先级**：每个新线程都从默认 nice(0) 和全核掩码出发，
  推理高峰时可能正好被迁移到小核上。

**改成常驻单线程执行器**：线程创建时就完成「绑大核 + 设 nice」，daemon 线程。

### 架构约束（别改错）

写 HTTP 服务那一侧的 `send()` 是在它自己的工作线程里把管道内容泵到 socket 的，
**这个线程必须一直活到响应写完，无法替换**。所以专用执行器承接的是
**延迟敏感的另一半**：跨线程调度与增量排空。网络写入仍由 HTTP 层负责，
两边通过管道解耦（建议 64KB 缓冲）。

### 三层削减 —— 作用在不同环节，不是重复

数据从模型到客户端要过四道关，每道关的固定开销在 token 级频率下都会显现：

| 层 | 削减对象 | 建议阈值 | 说明 |
|----|----------|----------|------|
| **JS 侧聚合** | RN 桥调用次数 | 20 字符 / 30ms | `emitDelta` 每次都是一趟完整的 JNI → Java → 队列写入，固定开销亚毫秒级；token 回调约 30~60ms 一次，逐 token 调用会让固定开销占掉可观比例 |
| **原生批量排空** | SSE 帧数 + TCP 分段 | 40ms 轮询窗口 | 一次轮询窗口内把所有已到达的分片合成一批，下游收到「约 40ms 一个大块」而不是「每 token 一个小块」 |
| **SSE 帧节流** | socket 写次数 | 12 字符 + 60ms | 攒够字符立即发，攒不够则由延迟上限兜底 |

**三层必须都存在** —— 它们分别削减桥调用、帧数、socket 写次数，不是同一件事的重复。

**延迟预算要算清楚**：最坏 30 + 40 ≈ 70ms 一帧。70ms 远低于人眼可辨的卡顿阈值
（约 100ms），观感仍是连续输出，而总调用次数下降接近一个数量级。

**JS 聚合用 `setTimeout` 而不是定时轮询**：定时轮询会在整个请求生命周期内
（长回答可能几分钟）持续唤醒 JS 线程；`setTimeout` 只在有未发送内容时存在。</br>
**注意：只在当前没有待发内容时挂定时器** —— 连续 push 会不断刷新缓冲，
重复挂会导致同一批被多次下发。

### 增量队列的两个要点

- **队列满时的行为是丢弃**（用 `offer` 而非 `put`），因为阻塞 JS 线程会连带
  卡住整个 UI。所以容量要放宽（建议 256），并且**把丢弃数计数上报** ——
  丢字表现为「回答中间少了几个字」，用户无从判断是模型问题还是服务问题，
  必须可观测。
- **复用同一个 `StringBuilder`** 做批次缓冲：每批次新建会让 GC 在 token 级
  频率下持续被打扰。

---

## 需求 5 · 关闭模型自动卸载（模型常驻内存）

### 要做什么

在局域网服务运行期间，**App 切到后台不要释放 llama 上下文**。

### 为什么

原逻辑通常是：`AppState` 从 `active` 切到 `background` 就调用
`releaseContext()` 释放上下文，并标记以便回前台时重载。
这对单机聊天是合理的省内存策略，但对**对外提供服务的场景是致命的**：

**App 一切到后台，正在服务的外部客户端会毫无征兆地开始收到 503**，
而服务本身还在监听 —— 用户看到的是「服务开着但不能用」。

### 技术要求（最小改动面）

- **只改自动释放的那个判定点**，例如改一个 getter 里的判断：

  ```
  服务运行中 && 用户要求常驻  →  返回 false（禁止自动释放）
  否则                        →  保持原有表达式，逐字不变
  ```

- **不碰原有的用户偏好字段**（`useAutoRelease` 之类），用户的设置保持原样。
- **不碰任何调用点签名**。
- **服务关闭或未开启常驻时，行为必须与改造前完全一致** —— 这是可验证的
  不变量，请在改动处注明。
- 做成**用户可见的开关**，不是一个悄悄打开的优化。关掉立刻回到原样。
- **模型卸载时同步释放内存锁定**，否则旧模型的页会被一直钉住，
  换模型后内存只增不减。

---

## 需求 6 · HTTP 层的 SSE / 单并发 / Keep-Alive

### 6.1 必须修掉「gzip 破坏 SSE」

**这是一个真实存在的、极易漏掉的缺陷。** 很多轻量 Java HTTP 服务器
（如 NanoHTTPD 2.3.1）的判断逻辑是：

```java
// 源码位置：NanoHTTPD.java:955
r.setGzipEncoding(useGzipWhenAccepted(r) && acceptEncoding != null
                  && acceptEncoding.contains("gzip"));

// 源码位置：NanoHTTPD.java:2140
protected boolean useGzipWhenAccepted(Response r) {
    return r.getMimeType() != null
        && (r.getMimeType().toLowerCase().contains("text/")    // ← SSE 会命中这里
            || r.getMimeType().toLowerCase().contains("/json"));
}
```

`text/event-stream` 含 `text/`，于是当客户端声明 `Accept-Encoding: gzip` 时，
响应体会被套进 `GZIPOutputStream`。后果：

- Deflater 在内部缓冲填满或 `finish()` 之前**不吐字节**
- 流式响应**永远不会 finish**（它是一个边产边发的管道）
- 结果就是「看起来在流式，其实一个 token 都收不到」，
  直到推理结束才一次性吐出全部内容

**为什么极难定位**：不带 `Accept-Encoding` 的裸 `curl` 一切正常，
所以接口自动化测试会全绿；而真实 SDK 全部静默退化
（例如 Python 的 `httpx` 默认就带 gzip）。现象是「接口通了但打字机效果没了」，
很容易被误归因到模型速度。

**修法**：在响应离开 handler 之前，对 `text/event-stream` 明确返回
不压缩，并在响应头上补：

```
Cache-Control: no-cache, no-transform
Connection: keep-alive
X-Accel-Buffering: no
```

`no-transform` 是必要的 —— 部分企业代理看到只有 `no-cache` 时仍会做 gzip 转码。

### 6.2 Keep-Alive 的真实代价：每连接一线程

轻量 HTTP 服务器的默认 `AsyncRunner` 常常是**每个连接一个新线程，不设上限**：

```java
++this.requestCount;
Thread t = new Thread(clientHandler);
t.setDaemon(true);
t.setName("NanoHttpd Request Processor (#" + this.requestCount + ")");
this.running.add(clientHandler);
t.start();
```

打开 keep-alive 之后这条路径会变成**资源泄漏**：一个客户端用 HTTP/1.1 连接池
开 20 条长连接、每条空闲几分钟，手机上就多出 20 个常驻线程。
低内存设备上这会直接把 App 推到系统低内存回收的名单里 —— 而我们的目标
恰恰是让模型常驻内存。

**改成有界线程池**：

- 线程数按「**在传输的请求**」而不是「并发连接数」计（建议 4）——
  keep-alive 的空闲连接不该占线程，只有真正在传输的请求才占用
- 队列满时用 `CallerRunsPolicy` **反压到 accept 循环**，
  而不是丢弃连接或抛 `RejectedExecutionException`
  （后者会让客户端看到莫名其妙的 connection reset）
- 停机时先关在途连接再关池，顺序反了会让响应被截断

### 6.3 socket 读超时的双重身份

很多服务器的 `start(timeout, daemon)` 参数**同时**决定两件事：

1. 请求体读取的等待上限
2. **keep-alive 空闲连接在下一轮读 header 上的阻塞时长**

原实现若传 300 秒，意味着一条闲置长连接会**占住一个工作线程整整五分钟**。
建议降到 **60 秒**：几 MB 的请求体在 WiFi 上远小于 60 秒就能传完，
而连接回收快了一个数量级。做成可配置项。

### 6.4 SSE 流式优化的三处

- **双阈值节流**。原来往往只有「攒够 N 字符就发」。模型中途停顿
  （思考、长 token）时，已攒的少量字符会一直压着不发，用户看到的是「卡住」。
  加一个**心跳线程**保证 `maxLatencyMs`（建议 60ms）内必定发出。
- **保活注释帧**。移动网络的 NAT/代理会掐掉长时间无数据的连接，
  而长思考（推理中间停顿十几秒）恰好会踩中。空闲超过 `heartbeatMs`
  （建议 15s，运营商 NAT 常见超时在 30~60s 量级）就发一个
  `: keep-alive` 注释帧 —— SSE 规范要求客户端忽略以 `:` 开头的内容，
  它只用来刷新链路活跃度。
- **直接写 UTF-8 字节**，不要用 `BufferedWriter(OutputStreamWriter(...))`：
  后者一次增量要经过字符编码器 + 8192 字符缓冲两层，而缓冲区不满时
  `flush()` 会强制推一次 —— 等于每帧都在做「编码 → 缓冲 → 立刻清空」的
  无用功。帧本身已经是完整的 UTF-8 字符串，`toByteArray(Charsets.UTF_8)`
  一次到位更直接。
- **先发一帧前导注释**（如 `: ok\n\n`），让客户端立刻确认流已建立。

### 6.5 单并发（这是刻意约束，不是权宜之计）

**同一时刻只允许一个推理在跑。** 理由：单模型场景下两个请求同时调用
llama 上下文会触发原生层竞态；8GB 机型上双份 KV cache 会直接 OOM。

实现要点：

- 一个许可 + 有界等待位（建议 4），超出直接返回 503，**不要无限堆积**
- **等待计数要语义准确**。如果用一个阻塞队列做计数，
  注意 `poll()` 取出的是**任意一个**元素而非本次放入的那个 ——
  因为元素没有身份（都是 `true`）所以计数上不会出错，
  但**无法回答「当前有几个请求在等」**，而这正是排查 503 时最需要的数字。
  改用原子计数器。
- **取不到许可要有超时**，不要无限等待：客户端早已断开时，
  无限等待会让这个 HTTP 工作线程永久占住，最终拖垮线程池。
- 提供一个健康检查端点，返回实时状态：
  `inference_busy` / `inference_waiting` / `inference_rejected` /
  `http_connections` / `http_threads`。
  排查「为什么我这边一直 503」时，这几个数字比任何日志都直接。

---

## 需求 7 · 机型适配：强制使用两个大核

### 要做什么

K20 上强制只用 2 个大核，并让线程数与之一致。

### 技术要点

- **「强制 N 核」的语义必须是「最高频的前 N 个核」**，而不是「整个高频簇」。
  实现方式：读所有在线 CPU 的 `cpuinfo_max_freq`，按频点**降序**取前 N 个
  （同频时按 CPU 编号升序，保证结果稳定可复现）。
  仍然**不要硬编码 `cpu6,cpu7`** —— 同代 SoC 的簇编号不保证一致。
- **为什么需要这个区分**：换成「4 大核」的机型（如部分骁龙 7 系），
  整簇绑定会得到 4 个线程，而用户要的是 2 个。
  更关键的是 —— **线程数超过物理大核数时吞吐反而下降**：
  ggml 的 matmul 有同步屏障，慢线程会拖住快线程。
- **`n_threads` 必须与「实际被绑定的核数」对齐**，不是整个高频簇的大小。
  两者必须来自同一个数字，否则会出现「绑了 2 个核但起了 8 个线程」——
  那比完全不调优**更慢**。
  对齐时取 `min(用户设置的线程数, 目标核数)`，尊重用户设的更小值。
- **默认值取 2**：绝大多数中端机的大核簇就是 2 个。
- **改成 0 表示「跟随高频簇」**（不强制），这是一个合法的模式。

### 验收

界面同时显示「强制核数」与「实际目标 CPU 列表」—— 前者是用户意图，
后者是设备实际给得出的结果。分开显示才能在「要 2 个但设备只有 1 个大核」时
如实说明，而不是显示成已按 2 核绑定。

---

## 需求 8 · 打开 App 后自动开启最佳性能模式

### 要做什么

用户打开 App 就自动生效，不需要手动开服务、手动点调优。

### 技术要点

**幂等**：内部用标记保证热重载 / 组件重复挂载时只跑一次，不重复绑核、
不重复启服务。

**执行顺序不能换**：

```
识别机型 → 参数写进配置 → 同步进原生配置 → 原子应用 → 最后才启服务
```

每一条顺序的理由：

1. **最后才启服务** —— 若先启服务再调优，第一个到来的请求会跑在未调优的
   线程上，用户看到的**首个响应就是慢的**。
2. **先把参数写进配置再调原生** —— 反过来的话原生已按新参数绑好核，
   而配置里还是旧值，界面与实况不一致，且下次改任何别的开关都会用旧参数
   **覆盖回去**。
3. **必须原子应用** —— 把「定目标核 → 绑大核 → 提优先级 → 压后台线程 →
   抬 rlimit → 降 oom 被杀概率」放在**一次调用**里完成，
   而不是让 JS 逐个调。因为中间态「已绑 2 个大核但 n_threads 还是 8」
   **比完全不调优更慢**。

**失败只记录不抛出** —— 调优属于旁路增强，它失败绝不能让 App 起不来。

---

## 需求 9 · 机型识别与推荐参数

### 要做什么

自动识别设备，给出一份可直接落地的「最佳性能模式」建议。

### 技术要点

- **判定顺序是「先看簇结构，再看 SoC 名」**：
  ```
  读 /proc/cpuinfo 的 Hardware 行      → SoC 型号（可能读不到）
  读 cpu<N>/cpufreq/cpuinfo_max_freq   → 簇结构（大核数 / 总核数）
  读 /proc/meminfo 的 MemTotal         → 内存 GB（向上取整）
  ```
  先看簇结构，是因为**它决定了能不能绑、绑几个**，这是性能的直接决定因素；
  SoC 名只用于生成更友好的标签。反过来做会出现在不认识的 SoC 上
  给不出任何建议的情况。
- **K20 预设**：SoC 含 `SDM730` / `SM7150`，**或**簇结构正好是
  「2 大 + 6 小 = 8 核」→ 强制 2 核、绑大核、开优先级、模型常驻、
  线程数对齐、SSE 参数取推荐值。
- **其他大小核机型** → 跟随高频簇（不强制核数），其余按通用值。
- **识别失败 → 通用配置且标记「可信度低」，不做大核绑定。**
  这一条很关键：**绑定未知核比不绑更糟**。所以识别不出时明确降级，
  并在界面上显示「未能识别机型，按通用配置应用」——
  **静默隐藏会让用户以为功能不存在**。
- **只产出建议，不直接改状态**：由上层决定是否套用。这样用户能看到
  「为什么给出这些值」（把理由文案一并返回），也能只应用其中一部分。
- **内存相关的保守处理**：内存较小的机型（如 < 6GB）默认**不开模型常驻**。

---

## 交付要求

1. **每条需求都要有可核对的生效判据**，并在界面上有一个「调优报告」区域
   把这些数值展示出来（请求值 / 实际值 / 被拒绝的原因）。
2. **报告里的线程明细**：提供一份进程内线程名快照（形如 `tid:name`），
   用于核对「到底绑到了谁」。llama.cpp 各版本的线程命名不一致，
   匹配不到时这是唯一的排查依据。
3. **在改动处写明「为什么这么做」**，特别是那些看起来可以简化、
   但简化后会变慢或用错的地方（例如：为什么必须是最高频前 N 个而不是整个簇；
   为什么 mlock 要自己补一半；为什么 gzip 必须为 SSE 关掉）。
